[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "sts2_simulator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pyzmq>=25.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["sts2_simulator*"]
